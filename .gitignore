# 依赖与构建产物
node_modules/
dist/
dist-electron/
release/
src-tauri/target/
claude-fast.exe
claude-fast.exe.WebView2/

# 运行时用户数据（清单/主题/用量台账/回收站，本地保留，不入库）
# 三者同层、同属数据根：仓库目录本身可能被认成便携根，产物落进来后 git add -A 会一并提交
# （trash/ 里是删过的会话原文）
config.json
config.json.bak
stats-ledger.json
stats-ledger.json.bak
trash/

# 启动脚本（含本机项目绝对路径，属用户数据，不入库）
scripts/

# 本机工具配置
.reasonix/
reasonix.toml

# 系统/编辑器
.DS_Store
Thumbs.db
*.log

# codegraph 代码图谱索引（本地重建产物，不入库）
.codegraph/

.zcode/

.workbuddy/
